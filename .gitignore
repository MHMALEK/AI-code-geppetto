.env
__pycache__/
*.pyc
.venv/
venv/
data/chroma/
data/tasks.db
data/*.db
data/screenshots/
data/*.lock
data/repos/
.DS_Store

# local dev docker overrides (contain host paths, not for version control)
docker-compose.override.yml

# sample-geppetto-repo is a separate standalone git repo — not part of this project
sample-geppetto-repo/
